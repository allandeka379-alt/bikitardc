// Project-wide constants

export const APP_NAME = 'Bikita RDC';
export const APP_VERSION = '0.1.0-demo';

/** How often the landing page polls the public-stats endpoint. */
export const STATS_POLL_MS = 30_000;

/** Scroll threshold (px) that swaps the landing nav from transparent to solid. */
export const NAV_SCROLL_THRESHOLD = 10;

/** Maximum content width — matches the Synvas rhythm at 1200px capped. */
export const MAX_CONTENT_PX = 1200;
